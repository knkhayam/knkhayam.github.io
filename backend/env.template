# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (comma-separated for multiple origins)
# For production, set to your frontend domain (e.g., https://knkhayam.github.io)
# For local development, include http://localhost:5500
ALLOWED_ORIGIN=https://knkhayam.github.io,http://localhost:5500,http://localhost:3000

